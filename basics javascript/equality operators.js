// const age = 19

// if (age === 18 ) //strict equality operator (==) is loose equality operator
//     {
//         console.log(`age is 18`);

//     }
//     else  
//     {
//         console.log(`age is not 18`);
//     }

const myAge = prompt ("please enter your age")

if (myAge >=18)
    {
        console.log(`you can drive :)`);
    }

    else 
    {
        console.log(`you can not drive`);
    }